/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
  basePath: "/nextjs-tailwind-portfolio-page",
};

module.exports = nextConfig;
